#ifndef TAG_H
#define TAG_H

#include <StandardDefines.h>

// Tag enum for different logging categories
DefineStandardTypes(Tag)
enum class Tag {
    Untagged    // Only one value for now
};

#endif // TAG_H

